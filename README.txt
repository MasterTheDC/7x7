7x7 Guide App
==============

前提
------
十字、バツ字、縦一列、横一列、3×3の出現比率がそれぞれ1/5, 1/5, 1/7, 1/7, 1/9と仮定しています。

補足
----
- `Undo` で一手戻せます。
- `Reset` で最初からやり直せます。
- `手動補正モード` を有効にすると、各マスを直接 ON/OFF できます。
- 推薦は事前生成済みの方策テーブルを優先し、未知状態では distilled policy で候補を絞った上で value-guided expectimax を回します。
